---
en:
  activerecord:
    errors:
      models:
        goal_account:
          attributes:
            base:
              account_already_fully_earmarked: This account is already fully allocated to “%{goal_name}”. Enter an amount to split it.
